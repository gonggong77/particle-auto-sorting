# Phase 2 Complete — 2026-04-24 03:03

## 전체 진행률
- 전체 17개 중 **6개 완료** (#16, #1, #2, #3, #4, #5)
- Phase 0 + Phase 1 + Phase 2(분석 레이어) 100% 완료

## 이 세션에서 완료한 작업

### ✅ #4 PrefabAnalyzer.cs
- 경로: `D:\particleSortingTest\Assets\Editor\Analysis\PrefabAnalyzer.cs`
- 네임스페이스: `ParticleAutoSorting.Editor.Analysis`
- 핵심 동작:
  - `Analyze(GameObject prefab)` → `PrefabData` 반환
  - 직속 자식에서 Above/Below 탐색 → 둘 다 없으면 HasAboveBelow=false + "Above/Below 구조 누락" 경고
  - Depth-first 순회로 Transform 수집 → GroupTag("Above"/"Below"/"Root") 부여
  - ParticleSystemRenderer 수집 (activeInHierarchy && enabled 필터)
  - `ParticleSystem.trails.enabled` 감지 → `Kind=Trail_Module`, HierarchyOrder=base+0.5, Material=psr.trailMaterial
  - TrailRenderer 별도 컴포넌트 수집 (`Kind=Trail_Component`)
  - 같은 GameObject에 Particle+Trail 공존 시 Trail은 +0.5 오프셋
  - `Mesh`는 renderMode==Mesh일 때만 설정 (그 외 null)
  - null Material → silent skip
  - 비활성 렌더러 → `"비활성 오브젝트 포함"` 경고 1회
  - Sub Emitter 참조 null → `"Sub Emitter 참조 누락"` 경고 1회
  - OilBefore/FudgeBefore 및 OilAfterAI/FudgeAfterAI 모두 현재값으로 초기화 (#6에서 after 값 덮어쓸 예정)
  - TrailRenderer의 sortingFudge는 public API가 없어서 SerializedObject의 `m_SortingFudge` 프로퍼티를 읽음 (없으면 0)

### ✅ #5 BatchCounter.cs
- 경로: `D:\particleSortingTest\Assets\Editor\Analysis\BatchCounter.cs`
- 네임스페이스: `ParticleAutoSorting.Editor.Analysis`
- API:
  - `CountPrefab(PrefabData data)` — 프리팹의 BatchBefore/BatchAfter 채움
  - `CountTotal(IEnumerable<PrefabData>)` → (before, after) 합산
- 알고리즘:
  - BatchKey = `(SortingLayerID, Material 참조, RenderMode, Mesh 참조)` — IEquatable 구현
  - SortKey = `(SortingLayerID, OiL, Fudge, HierarchyOrder)` 오름차순
  - GroupTag("Above"/"Below"/"Root") 단위로 버킷 분리 후 각 버킷에서 정렬 → 인접 BatchKey 변경 시 +1
  - `useBeforeValues` 플래그로 Before/After 두 번 계산
  - After 계산 시 `OilOverride/FudgeOverride` 우선, 없으면 `OilAfterAI/FudgeAfterAI` 사용

## 스펙과의 차이/주의사항
- TASKS.md의 BatchKey에는 `sortingLayerID`가 포함되어 있어 그대로 따름 (JSON 스펙은 `sortingLayerID` 제외지만 TASKS.md가 최신). 다른 SortingLayer 간 배치 묶임을 막으므로 안전한 방향.
- 비활성 오브젝트 경고는 개수 나열 대신 프리팹당 1회 "비활성 오브젝트 포함"만 추가 — 경고 pill 매칭이 단순해짐.

## 다음 작업 — Phase 3 시작
- **#6 SortingOptimizer.cs** — BlockedBy: #1, #2, #5 (모두 해제됨)
  - Material 그룹핑, OiL 할당(Above=char+1..+N, Below=char-1..-N)
  - OiL short 오버플로 시 `HasOverflow` 플래그 + 경고 (clamp 금지)
  - Fudge step = min(0.1, 0.9/(group_size-1))
  - 인터리브 감지(정렬 리스트 내 position 기반)

## 메모
- Unity 컴파일 검증은 여전히 미확인 (사용자 Editor에서 확인 필요)
- 현재 구조에서 SortingOptimizer가 채워질 때까지 BatchAfter == BatchBefore (AfterAI를 Before로 초기화했기 때문) — 의도된 동작
