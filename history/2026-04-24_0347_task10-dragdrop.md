# Task #10 — 드래그앤드롭 + 프리팹 등록 UI

- **완료 시각**: 2026-04-24 03:47
- **커밋 대상**: `D:\particleSortingTest\Assets\Editor\ParticleAutoSortingWindow.cs`
- **TASKS.md**: #10 [x]

## 구현 내용

- `DragAndDrop` API를 드롭존 Rect에 바인딩
  - `DragUpdated`: 유효한 프리팹 자산이 있을 때만 `Copy`, 아니면 `Rejected`
  - `DragPerform`: `AcceptDrag` 후 `RegisterPrefabs` 호출
- `ExtractPrefab`: `PrefabUtility.GetPrefabAssetType`로 Regular / Variant / Model만 통과 — 씬 오브젝트 및 일반 에셋 거부
- 중복 검사: `data.Prefab == prefab` 레퍼런스 비교, 중복 N개면 `DisplayDialog`로 "거부" 알림
- 등록 성공 시 `PrefabAnalyzer.Analyze` 즉시 호출하여 `PrefabData` 저장
- 리스트 엔트리: 파일명 + Renderer 수 + `×` 제거 버튼 (한 프레임 후 `RemoveAt`으로 enumeration 안전 처리)
- 상태 표시줄 메시지 갱신 ("분석 완료: N개 등록" / "프리팹 제거됨" / "프리팹 자산이 아닙니다")

## 스펙 대응

| 요구사항 | 대응 |
| --- | --- |
| DragAndDrop API로 등록 존 | `HandleDragAndDrop(Rect)` |
| 복수 등록 지원 | `DragAndDrop.objectReferences` 루프 |
| 중복 등록 시 경고 + 거부 | `IsAlreadyRegistered` + 합계 다이얼로그 |
| 등록 시 `PrefabAnalyzer.Analyze` 자동 호출 | `RegisterPrefabs` 내부 |
| 각 엔트리에 × 제거 버튼 | `DrawPrefabList` 헬프박스 행 |

## 남은 작업

- #11 프리팹 리스트 UI 정식 구현 (체크박스, 경고 pill, 배치 수 요약, 확장 토글)
- 현재 리스트는 파일명 + × 만 표시 — #11에서 본 엔트리 레이아웃으로 교체 예정
